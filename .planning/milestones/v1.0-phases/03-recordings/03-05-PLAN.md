---
phase: 03-recordings
plan: 05
type: execute
wave: 5
depends_on: ["03-03", "03-04"]
files_modified:
  - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs
  - src/BlackBoxBuddy/Views/RecordingsPage.axaml
  - src/BlackBoxBuddy/Views/RecordingDetailPage.axaml
  - src/BlackBoxBuddy/ViewModels/RecordingDetailViewModel.cs
  - tests/BlackBoxBuddy.Tests/ViewModels/RecordingsViewModelArchiveTests.cs
autonomous: false
requirements:
  - RECD-02
  - RECD-03
  - RECD-04
  - ARCH-01
  - ARCH-02
  - ARCH-03

must_haves:
  truths:
    - "User can enter multi-select mode via Select Clips button"
    - "User can check individual clips for archiving"
    - "User can archive selected clips with progress bar"
    - "Archived recordings show green Archived badge in the list"
    - "Archive progress overlay shows clip N of M progress"
    - "User can cancel an in-progress archive"
  artifacts:
    - path: "src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs"
      provides: "Multi-select mode, archive commands, archived state tracking"
      contains: "IsMultiSelectMode"
    - path: "src/BlackBoxBuddy/Views/RecordingsPage.axaml"
      provides: "Multi-select checkboxes, archived badges, Select/Done buttons"
      contains: "IsMultiSelectMode"
    - path: "src/BlackBoxBuddy/Views/RecordingDetailPage.axaml"
      provides: "Archive progress overlay"
      contains: "ArchiveProgress"
  key_links:
    - from: "src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs"
      to: "src/BlackBoxBuddy/Services/IArchiveService.cs"
      via: "Archive selected recordings"
      pattern: "_archiveService.ArchiveAsync"
    - from: "src/BlackBoxBuddy/Views/RecordingsPage.axaml"
      to: "src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs"
      via: "Multi-select checkboxes and archive button"
      pattern: "IsMultiSelectMode"
---

<objective>
Add multi-select archive flow, archived badges, archive progress overlay, and visual verification of the complete recording experience.

Purpose: This is the culmination of the phase's core value — users can select and preserve important footage before the dashcam overwrites it.
Output: Multi-select mode, archive commands with progress, archived badge display, visual verification of the entire recording workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-recordings/03-CONTEXT.md
@.planning/phases/03-recordings/03-RESEARCH.md
@.planning/phases/03-recordings/03-UI-SPEC.md
@.planning/phases/03-recordings/03-03-SUMMARY.md
@.planning/phases/03-recordings/03-04-SUMMARY.md

@src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs
@src/BlackBoxBuddy/Views/RecordingsPage.axaml
@src/BlackBoxBuddy/Views/RecordingDetailPage.axaml
@src/BlackBoxBuddy/ViewModels/RecordingDetailViewModel.cs

<interfaces>
From src/BlackBoxBuddy/Services/IArchiveService.cs:
```csharp
public interface IArchiveService
{
    Task ArchiveAsync(Recording recording, IProgress<double>? progress = null, CancellationToken ct = default);
    Task ArchiveTripAsync(TripGroup trip, IProgress<double>? progress = null, CancellationToken ct = default);
    string GetArchiveDirectory();
    bool IsArchived(Recording recording);
}
```

From src/BlackBoxBuddy/ViewModels/RecordingDetailViewModel.cs (created in Plan 04):
```csharp
// RecordingDetailViewModel already has CancelArchiveCommand with CancellationTokenSource.
// Plan 05 Task 2 adds archive progress overlay to RecordingDetailPage.axaml
// that binds to the existing CancelArchiveCommand — no new command needed.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-select mode, archive commands, and archived state tracking to RecordingsViewModel</name>
  <files>
    src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs
    tests/BlackBoxBuddy.Tests/ViewModels/RecordingsViewModelArchiveTests.cs
  </files>
  <read_first>
    src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs
    src/BlackBoxBuddy/Services/IArchiveService.cs
    src/BlackBoxBuddy/Models/Recording.cs
    src/BlackBoxBuddy/Models/TripGroup.cs
  </read_first>
  <behavior>
    - Test: ToggleMultiSelectCommand sets IsMultiSelectMode to true
    - Test: In multi-select mode, ToggleRecordingSelection adds/removes recording from SelectedRecordings
    - Test: SelectAllCommand adds all visible recordings to SelectedRecordings
    - Test: ArchiveSelectedCommand calls ArchiveAsync for each selected recording
    - Test: ArchiveSelectedCommand reports progress as completed/total ratio
    - Test: After archive, IsArchived flag is checked on each recording via ArchiveService
    - Test: SelectedCount reflects number of selected recordings
    - Test: CancelArchiveCommand cancels the archive CancellationTokenSource
    - Test: Exiting multi-select mode clears SelectedRecordings
  </behavior>
  <action>
    **Add to RecordingsViewModel (supplement existing code from Plan 03):**

    **New observable properties:**
    - `bool isMultiSelectMode` — toggles multi-select UI
    - `ObservableCollection<Recording> selectedRecordings` — currently selected items
    - `int selectedCount` — count of selected recordings
    - `bool isArchiving` — true during archive batch
    - `double archiveProgress` — 0.0 to 1.0 overall
    - `string archiveStatusText` — "Archiving clip N of M..."
    - `HashSet<string> archivedFileNames` — set of archived file names for quick lookup

    **New commands:**
    - `[RelayCommand] ToggleMultiSelect()` — toggle IsMultiSelectMode. When exiting, clear SelectedRecordings.
    - `[RelayCommand] ToggleRecordingSelection(Recording recording)` — if in SelectedRecordings, remove; else add. Update SelectedCount.
    - `[RelayCommand] SelectAll()` — add all Recording items from DisplayItems (flatten TripGroups) to SelectedRecordings. Update SelectedCount.
    - `[RelayCommand] async ArchiveSelected()` — iterate SelectedRecordings, call `_archiveService.ArchiveAsync()` for each with progress callback. Set `ArchiveStatusText = $"Archiving clip {i+1} of {total}..."`. After completion, refresh ArchivedFileNames, exit multi-select, call ApplyFilter to refresh badges.
    - `[RelayCommand] CancelArchive()` — cancel the CancellationTokenSource used by ArchiveSelected.

    **Archive state tracking:**
    After loading recordings (in LoadRecordingsAsync), populate `ArchivedFileNames` by checking `_archiveService.IsArchived(recording)` for each recording. This set is used by the UI to show/hide the "Archived" badge on each card.

    Add a method `IsRecordingArchived(Recording recording)` that checks `ArchivedFileNames.Contains(recording.FileName)` — used in XAML binding.

    **CancellationTokenSource management:**
    ```csharp
    private CancellationTokenSource? _archiveCts;

    [RelayCommand]
    private async Task ArchiveSelected()
    {
        _archiveCts = new CancellationTokenSource();
        IsArchiving = true;
        var selected = SelectedRecordings.ToList();
        try
        {
            for (int i = 0; i < selected.Count; i++)
            {
                _archiveCts.Token.ThrowIfCancellationRequested();
                ArchiveStatusText = $"Archiving clip {i + 1} of {selected.Count}...";
                ArchiveProgress = (double)i / selected.Count;
                await _archiveService.ArchiveAsync(selected[i], null, _archiveCts.Token);
            }
            ArchiveProgress = 1.0;
            ArchiveStatusText = "Archived successfully";
        }
        catch (OperationCanceledException)
        {
            ArchiveStatusText = "Archive cancelled";
        }
        finally
        {
            IsArchiving = false;
            _archiveCts?.Dispose();
            _archiveCts = null;
            // Refresh archived state
            foreach (var r in selected)
                if (_archiveService.IsArchived(r))
                    ArchivedFileNames.Add(r.FileName);
            IsMultiSelectMode = false;
            SelectedRecordings.Clear();
            OnPropertyChanged(nameof(SelectedCount));
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet test tests/BlackBoxBuddy.Tests --filter "FullyQualifiedName~RecordingsViewModelArchiveTests" --no-restore -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `isMultiSelectMode`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `ObservableCollection<Recording> selectedRecordings`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `ArchiveSelected`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `CancelArchive`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `SelectAll`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `ToggleRecordingSelection`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `ArchivedFileNames`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `Archiving clip`
    - tests/BlackBoxBuddy.Tests/ViewModels/RecordingsViewModelArchiveTests.cs has at least 6 test methods
    - All tests pass
  </acceptance_criteria>
  <done>RecordingsViewModel supports multi-select mode, batch archive with progress and cancel, archived state tracking per recording</done>
</task>

<task type="auto">
  <name>Task 2: Update RecordingsPage and RecordingDetailPage XAML with multi-select checkboxes, archived badges, archive progress overlay</name>
  <files>
    src/BlackBoxBuddy/Views/RecordingsPage.axaml
    src/BlackBoxBuddy/Views/RecordingDetailPage.axaml
  </files>
  <read_first>
    src/BlackBoxBuddy/Views/RecordingsPage.axaml
    src/BlackBoxBuddy/Views/RecordingDetailPage.axaml
    src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs
    src/BlackBoxBuddy/ViewModels/RecordingDetailViewModel.cs
    .planning/phases/03-recordings/03-UI-SPEC.md
  </read_first>
  <action>
    **Update RecordingsPage.axaml — add multi-select controls:**

    1. **Select/Done button bar** above the filter chips:
       Per D-16 and UI-SPEC copywriting:
       ```xml
       <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,8" Spacing="8">
         <!-- Not in multi-select mode -->
         <Button Content="Select Clips" Command="{Binding ToggleMultiSelectCommand}"
                 IsVisible="{Binding !IsMultiSelectMode}" FontSize="14" Padding="12,8"/>
         <!-- In multi-select mode -->
         <Button Content="Select All" Command="{Binding SelectAllCommand}"
                 IsVisible="{Binding IsMultiSelectMode}" FontSize="14" Padding="12,8"/>
         <Button Content="Done Selecting" Command="{Binding ToggleMultiSelectCommand}"
                 IsVisible="{Binding IsMultiSelectMode}" FontSize="14" Padding="12,8"/>
       </StackPanel>
       ```

    2. **Multi-select checkbox on each recording card:**
       Add to the existing Recording DataTemplate, overlaid on the thumbnail via Panel:
       ```xml
       <CheckBox IsVisible="{Binding $parent[ListBox].((vm:RecordingsViewModel)DataContext).IsMultiSelectMode}"
                 IsChecked="{Binding ..., Mode=OneWay}"
                 Command="{Binding $parent[ListBox].((vm:RecordingsViewModel)DataContext).ToggleRecordingSelectionCommand}"
                 CommandParameter="{Binding}"
                 HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4"/>
       ```
       The IsChecked binding checks if this recording is in SelectedRecordings. Since two-way binding to a collection membership is complex, use OneWay binding and handle selection entirely via command. The visual checked state can be managed via a converter that checks `SelectedRecordings.Contains(recording)`.

    3. **Archived badge on each recording card:**
       Per D-18 and UI-SPEC:
       ```xml
       <Border CornerRadius="4" Padding="4,2" Background="#4CAF50"
               HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="4"
               IsVisible="{Binding FileName, Converter={StaticResource ArchivedCheckConverter}}">
         <StackPanel Orientation="Horizontal" Spacing="4">
           <!-- Checkmark PathGeometry icon -->
           <TextBlock Text="Archived" FontSize="12" Foreground="White"/>
         </StackPanel>
       </Border>
       ```
       The `ArchivedCheckConverter` will need access to the ViewModel's ArchivedFileNames set. An alternative approach: instead of a converter, add an `IsArchived` property to a wrapping ViewModel for each list item, or use a `MultiBinding` with the ArchivedFileNames. **Simplest approach:** Create a `RecordingItemViewModel` wrapper with `IsArchived` property that RecordingsViewModel populates. OR use a lighter approach: add a `FuncValueConverter` in the page resources that captures the ViewModel.

       **Practical recommendation:** Since Avalonia binding can walk the tree, use a `MultiBinding` with the parent DataContext:
       ```xml
       <Border.IsVisible>
         <MultiBinding Converter="{StaticResource ArchivedMultiConverter}">
           <Binding Path="FileName"/>
           <Binding Path="$parent[ListBox].((vm:RecordingsViewModel)DataContext).ArchivedFileNames"/>
         </MultiBinding>
       </Border.IsVisible>
       ```
       Create a simple `ArchivedMultiConverter : IMultiValueConverter` that checks if the filename is in the set.

    4. **Archive Selected button** (bottom floating bar when in multi-select mode):
       Per UI-SPEC:
       ```xml
       <Border IsVisible="{Binding IsMultiSelectMode}" Background="#CC000000"
               HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Padding="16">
         <Button HorizontalAlignment="Stretch" Background="#2196F3"
                 Command="{Binding ArchiveSelectedCommand}"
                 IsEnabled="{Binding SelectedCount}">
           <TextBlock FontSize="14" Foreground="White">
             <TextBlock.Text>
               <MultiBinding StringFormat="Archive Selected ({0})">
                 <Binding Path="SelectedCount"/>
               </MultiBinding>
             </TextBlock.Text>
           </TextBlock>
         </Button>
       </Border>
       ```

    5. **Archive progress overlay** (on RecordingsPage, shown during batch archive):
       Per UI-SPEC:
       ```xml
       <Border IsVisible="{Binding IsArchiving}" Background="#CC000000"
               HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
         <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
           <TextBlock Text="{Binding ArchiveStatusText}" FontSize="14" Foreground="White"
                      HorizontalAlignment="Center"/>
           <ProgressBar Value="{Binding ArchiveProgress}" Minimum="0" Maximum="1"
                        Height="4" Width="200"/>
           <Button Content="Stop Archiving" Command="{Binding CancelArchiveCommand}"
                   HorizontalAlignment="Center" Padding="12,8"/>
         </StackPanel>
       </Border>
       ```

    **Update RecordingDetailPage.axaml — verify archive progress overlay exists:**
    Plan 03-04 Task 2 already adds the archive progress overlay with `CancelArchiveCommand` binding to RecordingDetailPage.axaml. Verify it is present. If not, add:
    ```xml
    <Border IsVisible="{Binding IsArchiving}" Background="#CC000000"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
        <TextBlock Text="Archiving..." FontSize="14" Foreground="White" HorizontalAlignment="Center"/>
        <ProgressBar Value="{Binding ArchiveProgress}" Minimum="0" Maximum="1"
                     Height="4" Width="200"/>
        <Button Content="Stop Archiving" Command="{Binding CancelArchiveCommand}"
                HorizontalAlignment="Center" Padding="12,8"/>
      </StackPanel>
    </Border>
    ```
    This binds to `CancelArchiveCommand` which exists on RecordingDetailViewModel (added in Plan 04 Task 1).
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet build src/BlackBoxBuddy/BlackBoxBuddy.csproj -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "Select Clips"
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "Done Selecting"
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "Select All"
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "Archive Selected"
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "IsMultiSelectMode"
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains `CheckBox`
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "Archived" (badge text)
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains `#4CAF50` (archived badge green)
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains "Stop Archiving"
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains `ArchiveProgress`
    - src/BlackBoxBuddy/Views/RecordingDetailPage.axaml contains `CancelArchiveCommand`
    - src/BlackBoxBuddy/Views/RecordingDetailPage.axaml contains `ArchiveProgress`
    - `dotnet build` succeeds
  </acceptance_criteria>
  <done>RecordingsPage has multi-select mode with checkboxes, archived badges, archive progress overlay with cancel; RecordingDetailPage has archive progress overlay bound to CancelArchiveCommand from RecordingDetailViewModel</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete recordings workflow</name>
  <files>n/a</files>
  <action>User verifies the complete Phase 3 recordings feature end-to-end by running the app and testing all flows.</action>
  <what-built>
    Complete Phase 3 recordings feature:
    1. Recording list with card layout showing thumbnails, metadata, event badges
    2. Event-type filter chips (All / Radar / G-Shock / Parking)
    3. Virtual trip grouping with expandable trip headers and nested clips
    4. Recording detail page with LibVLCSharp video playback
    5. Thumbnail shown before playback starts (RDTL-02)
    6. Extended player controls (play/pause, seek, speed, frame step)
    7. Dual-camera layout (side-by-side on desktop)
    8. Multi-select mode for batch archiving
    9. Archive progress overlay with cancel (from both list and detail pages)
    10. Archived recording badges
  </what-built>
  <how-to-verify>
    1. Run the app: `cd /home/hemu/src/bbb-app && dotnet run --project src/BlackBoxBuddy.Desktop`
    2. Navigate to the Recordings tab — verify recording cards appear with thumbnails and metadata
    3. Try each filter chip (All, Radar, G-Shock, Parking) — verify list updates
    4. Look for a trip group header — verify it shows aggregated stats and nested clips
    5. Tap a recording card — verify detail page opens with thumbnail visible before playback
    6. Tap play — verify video plays (sample.mp4 from embedded resource)
    7. Test player controls — play/pause, seek bar, speed buttons (0.5x, 1x, 2x)
    8. Navigate back to the list
    9. Tap "Select Clips" — verify multi-select mode with checkboxes
    10. Select a few clips and tap "Archive Selected" — verify progress overlay
    11. After archive, verify "Archived" badge appears on archived clips
    12. Verify responsive layout: resize window to check portrait/landscape adaptation
  </how-to-verify>
  <verify>User approval</verify>
  <done>User confirms all recording list, detail, archive features work correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build src/BlackBoxBuddy.sln` compiles
- `dotnet test tests/BlackBoxBuddy.Tests` — all tests pass
- Multi-select mode works with checkbox selection
- Archive downloads to ~/BlackBoxBuddy/Archives/ directory
- Archive progress overlay shows correctly
- Archived badges appear after archiving
</verification>

<success_criteria>
- Multi-select mode with "Select Clips"/"Done Selecting" buttons (D-16)
- Checkbox overlays on recording cards in multi-select mode (D-16)
- "Archive Selected (N)" button with count (D-16)
- Batch download with progress "Archiving clip N of M..." (D-17)
- Cancel archive support (D-17)
- Archived recordings show green "Archived" badge (D-18)
- Complete visual verification of the entire recording workflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-recordings/03-05-SUMMARY.md`
</output>
