---
phase: 03-recordings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlackBoxBuddy/Models/Recording.cs
  - src/BlackBoxBuddy/Models/EventType.cs
  - src/BlackBoxBuddy/Models/CameraChannel.cs
  - src/BlackBoxBuddy/Models/TripGroup.cs
  - src/BlackBoxBuddy/Device/IDeviceFileSystem.cs
  - src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs
  - src/BlackBoxBuddy/Device/Mock/MockThumbnailGenerator.cs
  - src/BlackBoxBuddy/Services/IMediaPlayerService.cs
  - src/BlackBoxBuddy/Assets/sample.mp4
  - src/BlackBoxBuddy/BlackBoxBuddy.csproj
  - src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj
  - Directory.Packages.props
  - tests/BlackBoxBuddy.Tests/Models/RecordingTests.cs
  - tests/BlackBoxBuddy.Tests/Device/MockDashcamDeviceRecordingsTests.cs
autonomous: true
requirements:
  - RECD-01
  - RECD-05
  - RECD-06
  - RECD-07
  - RECD-08
  - RECD-09
  - RECD-10
  - RECD-11

must_haves:
  truths:
    - "Recording data model contains all required metadata fields (filename, datetime, duration, filesize, speed, g-force, distance, event type, camera channel, thumbnail)"
    - "IDeviceFileSystem.ListRecordingsAsync returns IReadOnlyList<Recording> instead of IReadOnlyList<string>"
    - "MockDashcamDevice generates 15-20 recordings with realistic data via Bogus"
    - "Mock thumbnails are generated as in-memory solid-color BGRA pixel data"
    - "IMediaPlayerService defines Play, Pause, Stop, Seek, SetRate, NextFrame operations"
    - "LibVLCSharp packages are added to Directory.Packages.props and Desktop project"
    - "A real sample.mp4 is bundled as EmbeddedResource and MockDashcamDevice.DownloadFileAsync streams it (D-09)"
  artifacts:
    - path: "src/BlackBoxBuddy/Models/Recording.cs"
      provides: "Recording record type with all D-01 fields"
      contains: "public record Recording"
    - path: "src/BlackBoxBuddy/Models/EventType.cs"
      provides: "EventType enum per D-02"
      contains: "enum EventType"
    - path: "src/BlackBoxBuddy/Models/CameraChannel.cs"
      provides: "CameraChannel enum"
      contains: "enum CameraChannel"
    - path: "src/BlackBoxBuddy/Models/TripGroup.cs"
      provides: "TripGroup record for grouped recordings"
      contains: "public record TripGroup"
    - path: "src/BlackBoxBuddy/Device/IDeviceFileSystem.cs"
      provides: "Refactored interface with Recording return type"
      exports: ["ListRecordingsAsync"]
    - path: "src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs"
      provides: "Expanded mock with 15-20 Bogus recordings"
      contains: "Faker"
    - path: "src/BlackBoxBuddy/Services/IMediaPlayerService.cs"
      provides: "Full media player interface"
      contains: "Play"
    - path: "src/BlackBoxBuddy/Assets/sample.mp4"
      provides: "Real MP4 file for mock video playback (D-09)"
  key_links:
    - from: "src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs"
      to: "src/BlackBoxBuddy/Models/Recording.cs"
      via: "returns IReadOnlyList<Recording>"
      pattern: "IReadOnlyList<Recording>"
    - from: "src/BlackBoxBuddy/Device/IDeviceFileSystem.cs"
      to: "src/BlackBoxBuddy/Models/Recording.cs"
      via: "ListRecordingsAsync return type"
      pattern: "Task<IReadOnlyList<Recording>>"
    - from: "src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs"
      to: "src/BlackBoxBuddy/Assets/sample.mp4"
      via: "DownloadFileAsync streams the embedded MP4 resource"
      pattern: "GetManifestResourceStream"
---

<objective>
Define all data models, refactor device interfaces to use Recording records, expand MockDashcamDevice with Bogus-generated data, bundle a sample MP4 for mock playback, add NuGet packages for video playback, and expand IMediaPlayerService.

Purpose: Establish the data foundation everything in Phase 3 depends on — models, interfaces, mock data, sample video asset, and package references.
Output: Recording/EventType/CameraChannel/TripGroup models, refactored IDeviceFileSystem, expanded MockDashcamDevice with real MP4 streaming, expanded IMediaPlayerService, NuGet packages added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recordings/03-CONTEXT.md
@.planning/phases/03-recordings/03-RESEARCH.md

@src/BlackBoxBuddy/Device/IDeviceFileSystem.cs
@src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs
@src/BlackBoxBuddy/Services/IMediaPlayerService.cs
@src/BlackBoxBuddy/BlackBoxBuddy.csproj
@src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj
@Directory.Packages.props
@src/BlackBoxBuddy/Models/ConnectionState.cs

<interfaces>
<!-- Existing types the executor needs -->

From src/BlackBoxBuddy/Models/ConnectionState.cs:
```csharp
namespace BlackBoxBuddy.Models;
public enum ConnectionState { Disconnected, Searching, Connected, NeedsProvisioning }
```

From src/BlackBoxBuddy/Device/IDeviceFileSystem.cs:
```csharp
public interface IDeviceFileSystem
{
    Task<IReadOnlyList<string>> ListRecordingsAsync(CancellationToken ct = default);
    Task<Stream> DownloadFileAsync(string path, CancellationToken ct = default);
    Task<bool> DeleteFileAsync(string path, CancellationToken ct = default);
}
```

From src/BlackBoxBuddy/Services/IMediaPlayerService.cs:
```csharp
public interface IMediaPlayerService
{
    // Stub — implementations added in Phase 3
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create data models (Recording, EventType, CameraChannel, TripGroup) and refactor IDeviceFileSystem</name>
  <files>
    src/BlackBoxBuddy/Models/Recording.cs
    src/BlackBoxBuddy/Models/EventType.cs
    src/BlackBoxBuddy/Models/CameraChannel.cs
    src/BlackBoxBuddy/Models/TripGroup.cs
    src/BlackBoxBuddy/Device/IDeviceFileSystem.cs
    src/BlackBoxBuddy/Services/IMediaPlayerService.cs
    tests/BlackBoxBuddy.Tests/Models/RecordingTests.cs
  </files>
  <read_first>
    src/BlackBoxBuddy/Device/IDeviceFileSystem.cs
    src/BlackBoxBuddy/Services/IMediaPlayerService.cs
    src/BlackBoxBuddy/Models/ConnectionState.cs
    src/BlackBoxBuddy/Models/Settings/WifiSettings.cs
  </read_first>
  <behavior>
    - Test: Recording record has all 10 required properties accessible
    - Test: Recording value equality works (two records with same values are equal)
    - Test: EventType enum has exactly 4 values: None=0, Radar=1, GShock=2, Parking=3
    - Test: CameraChannel enum has Front, Rear, Both values
    - Test: TripGroup record calculates TotalDuration from sum of clip Durations
    - Test: TripGroup record calculates TotalDistance from sum of clip Distances
    - Test: TripGroup record calculates AvgSpeed as weighted average by duration
    - Test: TripGroup record calculates PeakGForce as max across clips
  </behavior>
  <action>
    Per D-01, create `src/BlackBoxBuddy/Models/Recording.cs`:
    ```csharp
    namespace BlackBoxBuddy.Models;
    public record Recording(
        string FileName,
        DateTime DateTime,
        TimeSpan Duration,
        long FileSize,
        double AvgSpeed,
        double PeakGForce,
        double Distance,
        EventType EventType,
        CameraChannel CameraChannel,
        byte[] ThumbnailData);
    ```

    Per D-02, create `src/BlackBoxBuddy/Models/EventType.cs`:
    ```csharp
    namespace BlackBoxBuddy.Models;
    public enum EventType { None = 0, Radar = 1, GShock = 2, Parking = 3 }
    ```

    Create `src/BlackBoxBuddy/Models/CameraChannel.cs`:
    ```csharp
    namespace BlackBoxBuddy.Models;
    public enum CameraChannel { Front, Rear, Both }
    ```

    Per D-13/D-14, create `src/BlackBoxBuddy/Models/TripGroup.cs`:
    ```csharp
    namespace BlackBoxBuddy.Models;
    public record TripGroup(IReadOnlyList<Recording> Clips)
    {
        public TimeSpan TotalDuration => Clips.Aggregate(TimeSpan.Zero, (sum, r) => sum + r.Duration);
        public double TotalDistance => Clips.Sum(r => r.Distance);
        public double AvgSpeed => TotalDuration.TotalSeconds > 0
            ? Clips.Sum(r => r.AvgSpeed * r.Duration.TotalSeconds) / TotalDuration.TotalSeconds
            : 0;
        public double PeakGForce => Clips.Max(r => r.PeakGForce);
        public DateTime StartTime => Clips.Min(r => r.DateTime);
        public DateTime EndTime => Clips.Max(r => r.DateTime + r.Duration);
    }
    ```

    Refactor `src/BlackBoxBuddy/Device/IDeviceFileSystem.cs` — change `ListRecordingsAsync` return type:
    ```csharp
    using BlackBoxBuddy.Models;
    namespace BlackBoxBuddy.Device;
    public interface IDeviceFileSystem
    {
        Task<IReadOnlyList<Recording>> ListRecordingsAsync(CancellationToken ct = default);
        Task<Stream> DownloadFileAsync(string path, CancellationToken ct = default);
        Task<bool> DeleteFileAsync(string path, CancellationToken ct = default);
    }
    ```

    Expand `src/BlackBoxBuddy/Services/IMediaPlayerService.cs` with full API:
    ```csharp
    namespace BlackBoxBuddy.Services;
    public interface IMediaPlayerService : IDisposable
    {
        object CreatePlayer();
        void Play(object player, Uri mediaUri);
        void Pause(object player);
        void Stop(object player);
        void Seek(object player, float position);
        void SetRate(object player, float rate);
        void NextFrame(object player);
        void PreviousFrame(object player);
        void DisposePlayer(object player);
    }
    ```
    Note: `object` is used for the player handle because the concrete type (LibVLC MediaPlayer) is in the Desktop project, not the shared project. The interface must remain platform-agnostic.
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet test tests/BlackBoxBuddy.Tests --filter "FullyQualifiedName~RecordingTests" --no-restore -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - src/BlackBoxBuddy/Models/Recording.cs contains `public record Recording(`
    - src/BlackBoxBuddy/Models/Recording.cs contains `string FileName`
    - src/BlackBoxBuddy/Models/Recording.cs contains `byte[] ThumbnailData`
    - src/BlackBoxBuddy/Models/EventType.cs contains `None = 0`
    - src/BlackBoxBuddy/Models/EventType.cs contains `GShock = 2`
    - src/BlackBoxBuddy/Models/CameraChannel.cs contains `enum CameraChannel`
    - src/BlackBoxBuddy/Models/TripGroup.cs contains `public record TripGroup`
    - src/BlackBoxBuddy/Models/TripGroup.cs contains `TotalDuration`
    - src/BlackBoxBuddy/Models/TripGroup.cs contains `PeakGForce`
    - src/BlackBoxBuddy/Device/IDeviceFileSystem.cs contains `Task<IReadOnlyList<Recording>>`
    - src/BlackBoxBuddy/Device/IDeviceFileSystem.cs does NOT contain `IReadOnlyList<string>`
    - src/BlackBoxBuddy/Services/IMediaPlayerService.cs contains `void Play(`
    - src/BlackBoxBuddy/Services/IMediaPlayerService.cs contains `void NextFrame(`
    - tests/BlackBoxBuddy.Tests/Models/RecordingTests.cs exists and all tests pass
  </acceptance_criteria>
  <done>All 4 model files exist with correct types, IDeviceFileSystem returns Recording records, IMediaPlayerService has full API, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Expand MockDashcamDevice with Bogus data, bundle sample.mp4, add MockThumbnailGenerator, add NuGet packages, fix compilation</name>
  <files>
    src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs
    src/BlackBoxBuddy/Device/Mock/MockThumbnailGenerator.cs
    src/BlackBoxBuddy/Assets/sample.mp4
    src/BlackBoxBuddy/BlackBoxBuddy.csproj
    src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj
    Directory.Packages.props
    tests/BlackBoxBuddy.Tests/Device/MockDashcamDeviceRecordingsTests.cs
  </files>
  <read_first>
    src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs
    src/BlackBoxBuddy/BlackBoxBuddy.csproj
    src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj
    Directory.Packages.props
    src/BlackBoxBuddy/Models/Recording.cs
    src/BlackBoxBuddy/Models/EventType.cs
    src/BlackBoxBuddy/Models/CameraChannel.cs
  </read_first>
  <action>
    **Per D-09, create and bundle a sample MP4 file as an embedded resource:**

    1. Generate a minimal valid MP4 file (~2-5 seconds) using ffmpeg:
       ```bash
       mkdir -p src/BlackBoxBuddy/Assets
       ffmpeg -f lavfi -i "color=c=black:s=320x180:d=3" -f lavfi -i "anullsrc=r=44100:cl=mono" -shortest -c:v libx264 -preset ultrafast -tune stillimage -pix_fmt yuv420p -c:a aac -b:a 32k -movflags +faststart src/BlackBoxBuddy/Assets/sample.mp4
       ```
       This creates a 3-second black video with silent audio — small file, valid MP4 container that LibVLCSharp can play.

    2. Add the MP4 as an EmbeddedResource in `src/BlackBoxBuddy/BlackBoxBuddy.csproj`:
       ```xml
       <ItemGroup>
         <EmbeddedResource Include="Assets/sample.mp4" LogicalName="BlackBoxBuddy.Assets.sample.mp4"/>
       </ItemGroup>
       ```

    3. Update `MockDashcamDevice.DownloadFileAsync` to stream the embedded sample.mp4:
       ```csharp
       public Task<Stream> DownloadFileAsync(string path, CancellationToken ct = default)
       {
           var assembly = typeof(MockDashcamDevice).Assembly;
           var stream = assembly.GetManifestResourceStream("BlackBoxBuddy.Assets.sample.mp4");
           if (stream is null)
               throw new FileNotFoundException($"Embedded resource sample.mp4 not found");
           return Task.FromResult<Stream>(stream);
       }
       ```
       All mock recordings return the same sample.mp4 stream regardless of the `path` parameter.

    4. Update all mock Recording `FileName` values to use names that end in `.mp4` (e.g., `CLIP_20260320_120000_F.mp4`) so downstream code recognizes them as video files.

    **NuGet packages — add to `Directory.Packages.props` inside the existing `<ItemGroup>`:**
    ```xml
    <PackageVersion Include="LibVLCSharp" Version="3.9.6"/>
    <PackageVersion Include="LibVLCSharp.Avalonia" Version="3.9.6"/>
    <PackageVersion Include="VideoLAN.LibVLC.Windows" Version="3.0.23"/>
    ```

    **Add Bogus to shared project `src/BlackBoxBuddy/BlackBoxBuddy.csproj`** — add inside the `<ItemGroup>` with PackageReferences:
    ```xml
    <PackageReference Include="Bogus"/>
    ```

    **Add LibVLCSharp to Desktop project `src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj`** — add new `<ItemGroup>`:
    ```xml
    <ItemGroup>
      <PackageReference Include="LibVLCSharp"/>
      <PackageReference Include="LibVLCSharp.Avalonia"/>
      <PackageReference Include="VideoLAN.LibVLC.Windows" Condition="$([MSBuild]::IsOSPlatform('Windows'))"/>
    </ItemGroup>
    ```
    Note: On Linux, the user must have `libvlc-dev` installed (e.g., `sudo pacman -S vlc` on Arch/CachyOS). This is documented but not gated.

    **Create `src/BlackBoxBuddy/Device/Mock/MockThumbnailGenerator.cs`:**
    Per D-04, generate solid-color 160x90 BGRA8888 pixel arrays. Color-coded by EventType:
    - None: (76, 175, 80, 255) — green
    - Radar: (255, 193, 7, 255) — amber (per UI-SPEC semantic colors)
    - GShock: (244, 67, 54, 255) — red
    - Parking: (255, 152, 0, 255) — orange

    ```csharp
    namespace BlackBoxBuddy.Device.Mock;
    using BlackBoxBuddy.Models;
    public static class MockThumbnailGenerator
    {
        public static byte[] GenerateSolidColor(int width, int height, EventType eventType)
        {
            var (r, g, b) = eventType switch
            {
                EventType.Radar => (255, 193, 7),
                EventType.GShock => (244, 67, 54),
                EventType.Parking => (255, 152, 0),
                _ => (76, 175, 80)
            };
            var pixels = new byte[width * height * 4];
            for (int i = 0; i < pixels.Length; i += 4)
            {
                pixels[i] = (byte)b;       // B
                pixels[i + 1] = (byte)g;    // G
                pixels[i + 2] = (byte)r;    // R
                pixels[i + 3] = 255;        // A
            }
            return pixels;
        }
    }
    ```

    **Refactor `MockDashcamDevice.cs`:**
    Per D-03, replace `List<string> _recordings` with `IReadOnlyList<Recording>` generated by Bogus.
    - Change field: `private readonly IReadOnlyList<Recording> _recordings;`
    - Initialize in constructor (not static): `_recordings = GenerateMockRecordings();`
    - Use seed 42 for deterministic data: `var faker = new Faker { Random = new Randomizer(42) };`
    - Generate 18 recordings spread across 3 days with:
      - First 6 recordings: consecutive sequence (timestamps 60 seconds apart) for trip grouping test
      - Remaining 12: spread randomly across 3 days
      - Event type weighted: 50% None, 20% Radar, 20% GShock, 10% Parking
      - Camera channels: mix of Front and Rear
      - Duration: 60-180 seconds
      - FileSize: duration * random(800000-1200000)
      - AvgSpeed: 0-120 km/h
      - PeakGForce: 0.1-3.5
      - Distance: duration * random(5-30) / 3600
      - ThumbnailData: MockThumbnailGenerator.GenerateSolidColor(160, 90, eventType)
    - Update `ListRecordingsAsync` to return `Task.FromResult<IReadOnlyList<Recording>>(_recordings)`
    - Update `WipeSdCardAsync` — change `_recordings` to a mutable `List<Recording>` field, or make _recordings a `List<Recording>` cast to `IReadOnlyList` on return
    - Fix any compilation errors from the IDeviceFileSystem interface change

    **Create test `tests/BlackBoxBuddy.Tests/Device/MockDashcamDeviceRecordingsTests.cs`:**
    - Test: ListRecordingsAsync returns 18 recordings
    - Test: At least 6 recordings have consecutive timestamps within 30 seconds (trip grouping material)
    - Test: Mix of EventTypes present (at least one Radar, one GShock, one Parking)
    - Test: All recordings have non-empty ThumbnailData of length 160*90*4 = 57600 bytes
    - Test: WipeSdCardAsync clears recordings
    - Test: DownloadFileAsync returns a non-empty Stream (the embedded sample.mp4) per D-09
    - Test: All Recording FileNames end with `.mp4`

    **Run `dotnet restore` and `dotnet build` to verify LibVLCSharp package compatibility with Avalonia 12 RC1.** If `LibVLCSharp.Avalonia` 3.9.6 fails, switch to `LibVLCSharp.Avalonia.Unofficial` version 3.9.6.1 in Directory.Packages.props (same VideoView API).
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet build src/BlackBoxBuddy/BlackBoxBuddy.csproj -v quiet && dotnet build src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj -v quiet && dotnet test tests/BlackBoxBuddy.Tests --filter "FullyQualifiedName~MockDashcamDeviceRecordingsTests" --no-restore -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - Directory.Packages.props contains `LibVLCSharp` Version="3.9.6"
    - Directory.Packages.props contains `VideoLAN.LibVLC.Windows` Version="3.0.23"
    - src/BlackBoxBuddy/BlackBoxBuddy.csproj contains `<PackageReference Include="Bogus"/>`
    - src/BlackBoxBuddy/BlackBoxBuddy.csproj contains `EmbeddedResource Include="Assets/sample.mp4"`
    - src/BlackBoxBuddy/Assets/sample.mp4 exists and is a valid MP4 file
    - src/BlackBoxBuddy.Desktop/BlackBoxBuddy.Desktop.csproj contains `<PackageReference Include="LibVLCSharp.Avalonia"/>`
    - src/BlackBoxBuddy/Device/Mock/MockThumbnailGenerator.cs contains `public static byte[] GenerateSolidColor`
    - src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs contains `Faker` (Bogus usage)
    - src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs contains `IReadOnlyList<Recording>`
    - src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs contains `GetManifestResourceStream` (D-09 embedded MP4 streaming)
    - src/BlackBoxBuddy/Device/Mock/MockDashcamDevice.cs does NOT contain `List<string> _recordings`
    - `dotnet build` succeeds for both shared and Desktop projects
    - All MockDashcamDeviceRecordingsTests pass including DownloadFileAsync stream test
  </acceptance_criteria>
  <done>MockDashcamDevice generates 18 rich Recording objects via Bogus with mock thumbnails, sample.mp4 bundled as EmbeddedResource and streamed by DownloadFileAsync (D-09), all NuGet packages added and building successfully, tests verify data generation and MP4 streaming</done>
</task>

</tasks>

<verification>
- `dotnet build src/BlackBoxBuddy.sln` compiles without errors
- `dotnet test tests/BlackBoxBuddy.Tests` — all existing tests still pass (no regressions from interface changes)
- MockDashcamDevice.ListRecordingsAsync returns Recording objects with all fields populated
- MockDashcamDevice.DownloadFileAsync returns a real MP4 stream (D-09)
- LibVLCSharp packages restore successfully
</verification>

<success_criteria>
- Recording record type exists with all 10 D-01 fields
- EventType enum has None, Radar, GShock, Parking (D-02)
- IDeviceFileSystem.ListRecordingsAsync returns IReadOnlyList<Recording> (D-01)
- MockDashcamDevice generates 18 recordings with Bogus (D-03)
- Mock thumbnails are solid-color BGRA pixel arrays (D-04)
- Sample.mp4 bundled as EmbeddedResource, MockDashcamDevice.DownloadFileAsync streams it (D-09)
- IMediaPlayerService defines full playback API
- LibVLCSharp + VideoLAN.LibVLC.Windows packages added
- All tests pass including new and existing
</success_criteria>

<output>
After completion, create `.planning/phases/03-recordings/03-01-SUMMARY.md`
</output>
