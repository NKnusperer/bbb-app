---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs
  - src/BlackBoxBuddy/ViewModels/DashboardViewModel.cs
  - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs
  - src/BlackBoxBuddy/ViewModels/LiveFeedViewModel.cs
  - src/BlackBoxBuddy/ViewModels/SettingsViewModel.cs
  - src/BlackBoxBuddy/Views/Shell/AppShellView.axaml
  - src/BlackBoxBuddy/Views/Shell/AppShellView.axaml.cs
  - src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml
  - src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml.cs
  - src/BlackBoxBuddy/Views/DashboardPage.axaml
  - src/BlackBoxBuddy/Views/DashboardPage.axaml.cs
  - src/BlackBoxBuddy/Views/RecordingsPage.axaml
  - src/BlackBoxBuddy/Views/RecordingsPage.axaml.cs
  - src/BlackBoxBuddy/Views/LiveFeedPage.axaml
  - src/BlackBoxBuddy/Views/LiveFeedPage.axaml.cs
  - src/BlackBoxBuddy/Views/SettingsPage.axaml
  - src/BlackBoxBuddy/Views/SettingsPage.axaml.cs
  - src/BlackBoxBuddy/Navigation/NavigationService.cs
  - src/BlackBoxBuddy/ViewLocator.cs
  - src/BlackBoxBuddy/AppServices.cs
  - src/BlackBoxBuddy/App.axaml.cs
  - src/BlackBoxBuddy/Views/MainWindow.axaml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "App launches showing a TabbedPage with 4 icon-only tabs"
    - "Tab placement is Auto — bottom on mobile, top/left on desktop"
    - "Connection indicator is always visible regardless of active tab"
    - "Connection indicator shows one of three visual states: Searching, Connected, Disconnected"
    - "Layout adapts at 600px breakpoint between portrait and landscape classes"
    - "Dark mode is the only theme"
  artifacts:
    - path: "src/BlackBoxBuddy/Views/Shell/AppShellView.axaml"
      provides: "TabbedPage root shell with connection indicator overlay"
      contains: "TabbedPage"
    - path: "src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml"
      provides: "Connection status display with state-specific visual styles"
      contains: "ConnectionState"
    - path: "src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs"
      provides: "Shell state management"
      contains: "ConnectionState"
    - path: "src/BlackBoxBuddy/Navigation/NavigationService.cs"
      provides: "Concrete navigation implementation"
      contains: "class NavigationService"
  key_links:
    - from: "src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs"
      to: "src/BlackBoxBuddy/Services/IDeviceService.cs"
      via: "constructor injection, subscribes to ConnectionStateChanged"
      pattern: "IDeviceService"
    - from: "src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml"
      to: "src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs"
      via: "DataContext binding to ConnectionState property"
      pattern: "Binding ConnectionState"
    - from: "src/BlackBoxBuddy/App.axaml.cs"
      to: "src/BlackBoxBuddy/Views/Shell/AppShellView.axaml"
      via: "MainWindow content set to AppShellView"
      pattern: "AppShellView"
---

<objective>
Build the app shell UI: TabbedPage with 4 icon-only tabs (Dashboard, Recordings, Live Feed, Settings), persistent connection indicator, responsive layout with 600px breakpoint, NavigationService implementation, and placeholder pages.

Purpose: Creates the visible app structure users interact with. All subsequent UI work (connection flow, provisioning) lives within this shell.
Output: Launchable app with dark-mode TabbedPage shell, 4 placeholder pages, connection indicator, responsive layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan uses -->

From src/BlackBoxBuddy/Models/ConnectionState.cs:
```csharp
public enum ConnectionState { Searching, Connected, Disconnected, NeedsProvisioning }
```

From src/BlackBoxBuddy/Models/DeviceInfo.cs:
```csharp
public class DeviceInfo
{
    public required string DeviceName { get; init; }
    public required string FirmwareVersion { get; init; }
    public bool IsProvisioned { get; set; }
    public string? IpAddress { get; init; }
}
```

From src/BlackBoxBuddy/Services/IDeviceService.cs:
```csharp
public interface IDeviceService
{
    Models.ConnectionState ConnectionState { get; }
    DeviceInfo? ConnectedDevice { get; }
    Task StartDiscoveryAsync(CancellationToken ct = default);
    Task<bool> ConnectManuallyAsync(string host, CancellationToken ct = default);
    Task DisconnectAsync(CancellationToken ct = default);
    event EventHandler<Models.ConnectionState>? ConnectionStateChanged;
}
```

From src/BlackBoxBuddy/Navigation/INavigationService.cs:
```csharp
public interface INavigationService
{
    Task PushAsync(ViewModelBase viewModel);
    Task PopAsync();
    Task PopToRootAsync();
}
```

From src/BlackBoxBuddy/ViewModels/ViewModelBase.cs:
```csharp
public abstract class ViewModelBase : ObservableObject { }
```

From src/BlackBoxBuddy/AppServices.cs:
```csharp
public static class AppServices
{
    public static IServiceProvider ConfigureServices(Action<IServiceCollection>? platformServices = null)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder ViewModels and pages for all 4 tabs</name>
  <files>
    src/BlackBoxBuddy/ViewModels/DashboardViewModel.cs,
    src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs,
    src/BlackBoxBuddy/ViewModels/LiveFeedViewModel.cs,
    src/BlackBoxBuddy/ViewModels/SettingsViewModel.cs,
    src/BlackBoxBuddy/Views/DashboardPage.axaml,
    src/BlackBoxBuddy/Views/DashboardPage.axaml.cs,
    src/BlackBoxBuddy/Views/RecordingsPage.axaml,
    src/BlackBoxBuddy/Views/RecordingsPage.axaml.cs,
    src/BlackBoxBuddy/Views/LiveFeedPage.axaml,
    src/BlackBoxBuddy/Views/LiveFeedPage.axaml.cs,
    src/BlackBoxBuddy/Views/SettingsPage.axaml,
    src/BlackBoxBuddy/Views/SettingsPage.axaml.cs
  </files>
  <read_first>
    src/BlackBoxBuddy/ViewModels/ViewModelBase.cs,
    src/BlackBoxBuddy/ViewModels/MainViewModel.cs,
    src/BlackBoxBuddy/Views/MainView.axaml,
    src/BlackBoxBuddy/Views/MainView.axaml.cs
  </read_first>
  <action>
Create 4 placeholder ViewModels that extend ViewModelBase. Each has a `Title` property. These are minimal stubs — Phase 2/3/4 fill them with real content.

1. src/BlackBoxBuddy/ViewModels/DashboardViewModel.cs:
```csharp
namespace BlackBoxBuddy.ViewModels;

public partial class DashboardViewModel : ViewModelBase
{
    public string Title => "Dashboard";
}
```

2. src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs:
```csharp
namespace BlackBoxBuddy.ViewModels;

public partial class RecordingsViewModel : ViewModelBase
{
    public string Title => "Recordings";
}
```

3. src/BlackBoxBuddy/ViewModels/LiveFeedViewModel.cs:
```csharp
namespace BlackBoxBuddy.ViewModels;

public partial class LiveFeedViewModel : ViewModelBase
{
    public string Title => "Live Feed";
}
```

4. src/BlackBoxBuddy/ViewModels/SettingsViewModel.cs:
```csharp
namespace BlackBoxBuddy.ViewModels;

public partial class SettingsViewModel : ViewModelBase
{
    public string Title => "Settings";
}
```

5. Create corresponding ContentPage AXAML files for each. Each page is a ContentPage (Avalonia 12 navigation type) with a centered TextBlock showing the title. Use `x:DataType` for compiled bindings. Example for DashboardPage.axaml:
```xml
<ContentPage xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:BlackBoxBuddy.ViewModels"
             x:Class="BlackBoxBuddy.Views.DashboardPage"
             x:DataType="vm:DashboardViewModel">
  <TextBlock Text="{Binding Title}"
             HorizontalAlignment="Center"
             VerticalAlignment="Center"
             FontSize="24"/>
</ContentPage>
```

6. Create code-behind for each page (.axaml.cs) — minimal, just InitializeComponent:
```csharp
using Avalonia.Controls;

namespace BlackBoxBuddy.Views;

public partial class DashboardPage : ContentPage
{
    public DashboardPage() => InitializeComponent();
}
```

Repeat the pattern for RecordingsPage, LiveFeedPage, SettingsPage — changing only the class name, DataType, and namespace as needed.

7. Run `dotnet build` to verify compilation.
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - src/BlackBoxBuddy/ViewModels/DashboardViewModel.cs contains `class DashboardViewModel : ViewModelBase`
    - src/BlackBoxBuddy/ViewModels/RecordingsViewModel.cs contains `class RecordingsViewModel : ViewModelBase`
    - src/BlackBoxBuddy/ViewModels/LiveFeedViewModel.cs contains `class LiveFeedViewModel : ViewModelBase`
    - src/BlackBoxBuddy/ViewModels/SettingsViewModel.cs contains `class SettingsViewModel : ViewModelBase`
    - src/BlackBoxBuddy/Views/DashboardPage.axaml contains `ContentPage`
    - src/BlackBoxBuddy/Views/RecordingsPage.axaml contains `ContentPage`
    - src/BlackBoxBuddy/Views/LiveFeedPage.axaml contains `ContentPage`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `ContentPage`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>4 placeholder pages and ViewModels exist, all extending ContentPage and ViewModelBase respectively. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build AppShellView with TabbedPage, connection indicator, responsive layout, and NavigationService</name>
  <!-- NOTE: This task touches 10 files but they form a single cohesive shell build.
       Half are 1-line code-behinds (.axaml.cs) and DI/ViewLocator updates.
       Splitting would create artificial boundaries within the shell wiring. -->
  <files>
    src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs,
    src/BlackBoxBuddy/Views/Shell/AppShellView.axaml,
    src/BlackBoxBuddy/Views/Shell/AppShellView.axaml.cs,
    src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml,
    src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml.cs,
    src/BlackBoxBuddy/Navigation/NavigationService.cs,
    src/BlackBoxBuddy/ViewLocator.cs,
    src/BlackBoxBuddy/AppServices.cs,
    src/BlackBoxBuddy/App.axaml.cs,
    src/BlackBoxBuddy/Views/MainWindow.axaml
  </files>
  <read_first>
    src/BlackBoxBuddy/ViewLocator.cs,
    src/BlackBoxBuddy/AppServices.cs,
    src/BlackBoxBuddy/App.axaml.cs,
    src/BlackBoxBuddy/Views/MainWindow.axaml,
    src/BlackBoxBuddy/Navigation/INavigationService.cs,
    src/BlackBoxBuddy/Services/IDeviceService.cs,
    src/BlackBoxBuddy/Models/ConnectionState.cs,
    src/BlackBoxBuddy/Views/DashboardPage.axaml
  </read_first>
  <action>
1. Create src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs per D-03, D-11:
```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using BlackBoxBuddy.Models;
using BlackBoxBuddy.Services;
using BlackBoxBuddy.Navigation;

namespace BlackBoxBuddy.ViewModels.Shell;

public partial class AppShellViewModel : ViewModelBase
{
    private readonly IDeviceService _deviceService;
    private readonly INavigationService _navigationService;

    [ObservableProperty]
    private ConnectionState _connectionState = ConnectionState.Searching;

    [ObservableProperty]
    private string _connectedDeviceName = string.Empty;

    [ObservableProperty]
    private string _connectionStatusText = "Searching...";

    public AppShellViewModel(IDeviceService deviceService, INavigationService navigationService)
    {
        _deviceService = deviceService;
        _navigationService = navigationService;
        _deviceService.ConnectionStateChanged += OnConnectionStateChanged;
    }

    private void OnConnectionStateChanged(object? sender, ConnectionState state)
    {
        ConnectionState = state;
        ConnectionStatusText = state switch
        {
            ConnectionState.Searching => "Searching...",
            ConnectionState.Connected => _deviceService.ConnectedDevice?.DeviceName ?? "Connected",
            ConnectionState.NeedsProvisioning => _deviceService.ConnectedDevice?.DeviceName ?? "Setup Required",
            ConnectionState.Disconnected => "No device found",
            _ => "Unknown"
        };
        ConnectedDeviceName = _deviceService.ConnectedDevice?.DeviceName ?? string.Empty;
    }

    [RelayCommand]
    private async Task StartDiscoveryAsync()
    {
        await _deviceService.StartDiscoveryAsync();
    }

    [RelayCommand]
    private async Task ConnectionIndicatorTappedAsync()
    {
        // Tap on indicator opens manual connection (CONN-03) or retries
        // Full implementation in Plan 04
        if (ConnectionState == ConnectionState.Disconnected)
        {
            await _deviceService.StartDiscoveryAsync();
        }
    }
}
```

2. Create src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml — a UserControl showing connection state per D-03, D-11. MUST have distinct visual states for each ConnectionState using Avalonia Styles with DataTriggers or class-based selectors. The Ellipse fill color MUST change per state:
- Searching: yellow (#FFFFC107)
- Connected: green (#FF4CAF50)
- NeedsProvisioning: orange (#FFFF9800)
- Disconnected: red (#FFF44336)

```xml
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:BlackBoxBuddy.ViewModels.Shell"
             xmlns:models="using:BlackBoxBuddy.Models"
             x:Class="BlackBoxBuddy.Views.Shell.ConnectionIndicator"
             x:DataType="vm:AppShellViewModel">
  <Border Padding="8,4" CornerRadius="4" Background="#1EFFFFFF">
    <StackPanel Orientation="Horizontal" Spacing="8">
      <!-- State dot — color changes per ConnectionState -->
      <Ellipse Width="10" Height="10" VerticalAlignment="Center">
        <Ellipse.Styles>
          <Style Selector="Ellipse">
            <Setter Property="Fill" Value="#FF666666"/>
          </Style>
        </Ellipse.Styles>
      </Ellipse>
      <!-- Status text -->
      <TextBlock Text="{Binding ConnectionStatusText}"
                 VerticalAlignment="Center"
                 FontSize="13"/>
      <!-- Searching indicator -->
      <ProgressBar IsIndeterminate="True"
                   IsVisible="{Binding ConnectionState, Converter={x:Static ObjectConverters.IsNotNull}}"
                   Height="2" Width="40"
                   VerticalAlignment="Center"/>
    </StackPanel>
  </Border>
</UserControl>
```

IMPORTANT: The above is a starting template. The executor MUST implement state-specific visual switching for the Ellipse Fill. Options:
(a) Use Avalonia DataTriggers if available in RC1, OR
(b) Use a value converter that maps ConnectionState enum to SolidColorBrush, OR
(c) Use multiple Ellipses with IsVisible bindings (one per state).

The key requirement is that the dot color visibly changes for each state. Use these exact colors:
- Searching: #FFFFC107 (yellow)
- Connected: #FF4CAF50 (green)
- NeedsProvisioning: #FFFF9800 (orange)
- Disconnected: #FFF44336 (red)

Add a PointerPressed handler or use Button wrapping to call ConnectionIndicatorTappedCommand.

3. Create ConnectionIndicator.axaml.cs (minimal code-behind):
```csharp
using Avalonia.Controls;

namespace BlackBoxBuddy.Views.Shell;

public partial class ConnectionIndicator : UserControl
{
    public ConnectionIndicator() => InitializeComponent();
}
```

4. Create src/BlackBoxBuddy/Views/Shell/AppShellView.axaml per D-01, D-02, D-03, D-04. The root is a Grid that contains a TabbedPage (TabPlacement="Auto") and the ConnectionIndicator overlaid at the top per D-03. Use AdaptiveBehavior for 600px breakpoint per D-04, FOUND-05, FOUND-06:
```xml
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:BlackBoxBuddy.ViewModels.Shell"
             xmlns:shell="using:BlackBoxBuddy.Views.Shell"
             xmlns:views="using:BlackBoxBuddy.Views"
             xmlns:i="using:Avalonia.Xaml.Interactions.Core"
             xmlns:ia="using:Avalonia.Xaml.Interactivity"
             x:Class="BlackBoxBuddy.Views.Shell.AppShellView"
             x:DataType="vm:AppShellViewModel">

  <ia:Interaction.Behaviors>
    <i:AdaptiveBehavior>
      <i:AdaptiveClassSetter MaxWidth="599" ClassName="portrait"/>
      <i:AdaptiveClassSetter MinWidth="600" ClassName="landscape"/>
    </i:AdaptiveBehavior>
  </ia:Interaction.Behaviors>

  <Grid RowDefinitions="Auto,*">
    <!-- Connection indicator — always visible per D-03 -->
    <shell:ConnectionIndicator Grid.Row="0"
                                DataContext="{Binding}"
                                HorizontalAlignment="Center"
                                Margin="0,4"/>

    <!-- TabbedPage with 4 icon-only tabs per D-01, D-02 -->
    <TabbedPage Grid.Row="1" TabPlacement="Auto">
      <ContentPage>
        <views:DashboardPage/>
      </ContentPage>
      <ContentPage>
        <views:RecordingsPage/>
      </ContentPage>
      <ContentPage>
        <views:LiveFeedPage/>
      </ContentPage>
      <ContentPage>
        <views:SettingsPage/>
      </ContentPage>
    </TabbedPage>
  </Grid>
</UserControl>
```

IMPORTANT: The exact TabbedPage XAML API may need adjustment. Avalonia 12 RC1's TabbedPage expects its children to be ContentPage instances directly. The Icon property on each ContentPage sets the tab icon. Consult the Avalonia 12 docs at https://docs.avaloniaui.net/controls/navigation/tabbedpage if the above pattern does not compile. Icon-only tabs per D-02 — either omit `Header` property or style tab item template to hide text.

For icons, use Avalonia's built-in PathGeometry or StreamGeometry resources. Create icon resources (Material Design style) in AppShellView or App.axaml resources. Use recognizable icons:
- Dashboard: grid/home icon
- Recordings: film/video icon
- Live Feed: camera/eye icon
- Settings: gear icon

5. Create AppShellView.axaml.cs:
```csharp
using Avalonia.Controls;

namespace BlackBoxBuddy.Views.Shell;

public partial class AppShellView : UserControl
{
    public AppShellView() => InitializeComponent();
}
```

6. Create src/BlackBoxBuddy/Navigation/NavigationService.cs — concrete implementation per D-24. Wraps Avalonia 12 NavigationPage push/pop. For now, a minimal implementation that the provisioning wizard (Plan 04) will use:
```csharp
using BlackBoxBuddy.ViewModels;

namespace BlackBoxBuddy.Navigation;

public class NavigationService : INavigationService
{
    // NavigationPage reference will be set when the shell initializes
    // Full implementation wired in Plan 04 when NavigationPage is used for provisioning

    public Task PushAsync(ViewModelBase viewModel)
    {
        // TODO: Implement with Avalonia 12 NavigationPage in Plan 04
        return Task.CompletedTask;
    }

    public Task PopAsync()
    {
        return Task.CompletedTask;
    }

    public Task PopToRootAsync()
    {
        return Task.CompletedTask;
    }
}
```

7. Update src/BlackBoxBuddy/ViewLocator.cs to add mappings for all new ViewModels:
```csharp
AppShellViewModel => new Views.Shell.AppShellView(),
DashboardViewModel => new Views.DashboardPage(),
RecordingsViewModel => new Views.RecordingsPage(),
LiveFeedViewModel => new Views.LiveFeedPage(),
SettingsViewModel => new Views.SettingsPage(),
```
Add the necessary `using BlackBoxBuddy.ViewModels.Shell;` import.

8. Update src/BlackBoxBuddy/AppServices.cs — add NavigationService registration and ViewModel registrations:
```csharp
// After existing service registrations:
services.AddSingleton<INavigationService, NavigationService>();

// Transient ViewModels
services.AddTransient<AppShellViewModel>();
services.AddTransient<DashboardViewModel>();
services.AddTransient<RecordingsViewModel>();
services.AddTransient<LiveFeedViewModel>();
services.AddTransient<SettingsViewModel>();
```
Add the necessary using statements for `BlackBoxBuddy.Navigation`, `BlackBoxBuddy.ViewModels`, `BlackBoxBuddy.ViewModels.Shell`.

9. Update src/BlackBoxBuddy/App.axaml.cs — change the main content from MainWindow/MainView to use AppShellView with AppShellViewModel resolved from DI:
```csharp
var shellVm = Ioc.Default.GetRequiredService<AppShellViewModel>();

if (ApplicationLifetime is IClassicDesktopStyleApplicationLifetime desktop)
{
    desktop.MainWindow = new MainWindow { Content = new Views.Shell.AppShellView { DataContext = shellVm } };
}
else if (ApplicationLifetime is IActivityApplicationLifetime activityLifetime)
{
    activityLifetime.MainViewFactory = () => new Views.Shell.AppShellView { DataContext = shellVm };
}
else if (ApplicationLifetime is ISingleViewApplicationLifetime singleViewPlatform)
{
    singleViewPlatform.MainView = new Views.Shell.AppShellView { DataContext = shellVm };
}
```

10. Update src/BlackBoxBuddy/Views/MainWindow.axaml — remove the embedded `<views:MainView />` content since content is now set programmatically. Keep Window shell (title, icon):
```xml
<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="BlackBoxBuddy.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="BlackBoxBuddy">
</Window>
```

11. Run `dotnet build` to verify compilation. If TabbedPage XAML does not compile as shown, adjust based on the actual Avalonia 12 RC1 API — the key requirement is TabPlacement="Auto" with 4 icon-only ContentPage children.
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet build</automated>
  </verify>
  <acceptance_criteria>
    - src/BlackBoxBuddy/ViewModels/Shell/AppShellViewModel.cs contains `class AppShellViewModel : ViewModelBase`
    - AppShellViewModel.cs contains `IDeviceService`
    - AppShellViewModel.cs contains `ConnectionState _connectionState`
    - AppShellViewModel.cs contains `ConnectionStateChanged`
    - src/BlackBoxBuddy/Views/Shell/AppShellView.axaml contains `TabbedPage`
    - AppShellView.axaml contains `TabPlacement`
    - AppShellView.axaml contains `ConnectionIndicator`
    - AppShellView.axaml contains `AdaptiveBehavior`
    - AppShellView.axaml contains `MaxWidth="599"`
    - AppShellView.axaml contains `MinWidth="600"`
    - src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml contains `ConnectionStatusText`
    - ConnectionIndicator.axaml contains state-specific Fill values or a converter for Ellipse color (at least one of: `#FF4CAF50`, `#FFF44336`, `#FFFFC107`, or a ConnectionState-to-Brush converter)
    - src/BlackBoxBuddy/Navigation/NavigationService.cs contains `class NavigationService : INavigationService`
    - ViewLocator.cs contains `AppShellViewModel`
    - ViewLocator.cs contains `DashboardViewModel`
    - AppServices.cs contains `AddSingleton<INavigationService, NavigationService>`
    - AppServices.cs contains `AddTransient<AppShellViewModel>`
    - App.axaml.cs contains `GetRequiredService<AppShellViewModel>`
    - App.axaml.cs contains `AppShellView`
    - `dotnet build` exits 0
  </acceptance_criteria>
  <done>App launches with dark-mode TabbedPage shell showing 4 icon-only tabs, connection indicator always visible with state-specific Ellipse colors (yellow/green/orange/red), responsive layout with 600px AdaptiveBehavior breakpoint. NavigationService registered. All ViewModels resolved from DI.</done>
</task>

</tasks>

<verification>
- `dotnet build` succeeds
- `dotnet test` passes (existing tests unbroken)
- App.axaml.cs resolves AppShellViewModel from DI and sets it as root
- TabbedPage has TabPlacement="Auto" with 4 ContentPage children
- ConnectionIndicator is outside tab content area (in Grid.Row="0")
- ConnectionIndicator Ellipse has distinct visual styles for each ConnectionState
- AdaptiveBehavior with 600px breakpoint is applied
</verification>

<success_criteria>
- Launchable app with dark mode TabbedPage shell
- 4 icon-only tabs: Dashboard, Recordings, Live Feed, Settings
- Connection indicator visible at all times showing state text
- Connection indicator Ellipse changes color per state (Searching=yellow, Connected=green, Disconnected=red)
- Responsive layout adapts at 600px breakpoint
- NavigationService implementation registered in DI
- All ViewModels resolved from DI container
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
