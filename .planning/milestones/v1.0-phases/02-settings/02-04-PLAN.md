---
phase: 02-settings
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/BlackBoxBuddy/Views/SettingsPage.axaml
  - src/BlackBoxBuddy/Views/SettingsPage.axaml.cs
  - src/BlackBoxBuddy/Converters/EnumBooleanConverter.cs
  - tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs
  - tests/BlackBoxBuddy.Tests/Views/SettingsPageTests.cs
autonomous: true
requirements:
  - WIFI-01
  - WIFI-02
  - WIFI-03
  - RMOD-01
  - RMOD-02
  - RMOD-03
  - RMOD-04
  - CHAN-01
  - CHAN-02
  - CAMR-01
  - CAMR-02
  - SENS-01
  - SENS-02
  - SENS-03
  - SYST-01
  - SYST-02
  - SYST-03
  - OVRL-01
  - OVRL-02
  - OVRL-03
  - OVRL-04
  - OVRL-05
  - DNGR-01
  - DNGR-02

must_haves:
  truths:
    - "Settings page has 8 visual sections (WiFi, Recording, Channels, Camera, Sensors, System, Overlays, Danger Zone) all visible by scrolling"
    - "Save bar is sticky at the bottom, outside the ScrollViewer, visible only when HasUnsavedChanges"
    - "Danger Zone section has red border and distinct background to signal destructiveness"
    - "All controls bind two-way to SettingsViewModel properties"
    - "Speed Unit selector is only visible when Speed Overlay toggle is on"
    - "Sensor sliders snap to integer ticks 1-5"
    - "Speaker volume slider snaps to integer ticks 0-5"
    - "EnumBooleanConverter is tested via [Fact] unit tests"
    - "SettingsPage loads without error in headless UI test via [AvaloniaFact]"
  artifacts:
    - path: "src/BlackBoxBuddy/Views/SettingsPage.axaml"
      provides: "Full settings UI with all 8 sections"
      min_lines: 200
    - path: "src/BlackBoxBuddy/Views/SettingsPage.axaml.cs"
      provides: "Clean code-behind triggering LoadSettings on Loaded"
      contains: "LoadSettingsCommand"
    - path: "src/BlackBoxBuddy/Converters/EnumBooleanConverter.cs"
      provides: "Reusable IValueConverter for RadioButton-to-enum binding"
      contains: "IValueConverter"
    - path: "tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs"
      provides: "Unit tests for EnumBooleanConverter"
      contains: "[Fact]"
    - path: "tests/BlackBoxBuddy.Tests/Views/SettingsPageTests.cs"
      provides: "Headless UI smoke test for SettingsPage"
      contains: "[AvaloniaFact]"
  key_links:
    - from: "src/BlackBoxBuddy/Views/SettingsPage.axaml"
      to: "src/BlackBoxBuddy/ViewModels/SettingsViewModel.cs"
      via: "x:DataType + two-way bindings"
      pattern: "Binding WifiBand|Binding DrivingMode|Binding HasUnsavedChanges"
    - from: "src/BlackBoxBuddy/Views/SettingsPage.axaml"
      to: "SettingsViewModel.SaveCommand"
      via: "Save button Command binding"
      pattern: "Command=.*SaveCommand"
    - from: "src/BlackBoxBuddy/Views/SettingsPage.axaml"
      to: "SettingsViewModel.FactoryResetCommand"
      via: "Danger Zone button Command binding"
      pattern: "Command=.*FactoryResetCommand"
---

<objective>
Build the complete Settings page XAML with all 8 sections, sticky Save bar, loading/error states, Danger Zone styling, and add tests for EnumBooleanConverter and headless SettingsPage smoke test.

Purpose: This is the visual implementation of every settings requirement. All controls bind to SettingsViewModel properties created in Plan 03. The layout follows the UI-SPEC contract. Per D-19, ValueConverters and Views must have automated test coverage — this plan includes both converter unit tests and a headless View smoke test.
Output: SettingsPage.axaml with ~250 lines of XAML, clean code-behind, EnumBooleanConverter with tests, headless SettingsPage smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-settings/02-CONTEXT.md
@.planning/phases/02-settings/02-UI-SPEC.md
@.planning/phases/02-settings/02-01-SUMMARY.md
@.planning/phases/02-settings/02-02-SUMMARY.md

<interfaces>
<!-- SettingsViewModel properties this XAML binds to (from Plan 03 spec) -->

```csharp
// Properties the XAML must bind to (x:DataType="vm:SettingsViewModel"):
// WiFi
WifiBand (enum: TwoPointFourGHz, FiveGHz) — RadioButton binding
WifiMode (enum: AccessPoint, Client) — RadioButton binding
WifiSsid (string) — TextBox binding
WifiPassword (string) — TextBox with PasswordChar binding

// Recording
DrivingMode (enum: Standard, Racing) — RadioButton binding
ParkingMode (enum: Standard, EventOnly) — RadioButton binding

// Channels
Channels (enum: FrontOnly, FrontAndRear) — RadioButton binding

// Camera
RearOrientation (enum: Normal, Flipped) — RadioButton binding

// Sensors
DrivingShockSensitivity (int 1-5) — Slider binding
ParkingShockSensitivity (int 1-5) — Slider binding
RadarSensitivity (int 1-5) — Slider binding

// System
GpsEnabled (bool) — ToggleSwitch binding
MicrophoneEnabled (bool) — ToggleSwitch binding
SpeakerVolume (int 0-5) — Slider binding (0=Disabled)

// Overlays
DateOverlayEnabled (bool) — ToggleSwitch binding
TimeOverlayEnabled (bool) — ToggleSwitch binding
GpsPositionOverlayEnabled (bool) — ToggleSwitch binding
SpeedOverlayEnabled (bool) — ToggleSwitch binding
SpeedUnit (enum: KilometersPerHour, MilesPerHour) — ComboBox binding

// State
IsLoading (bool) — loading overlay visibility
LoadError (string?) — error message visibility
HasUnsavedChanges (bool) — Save bar visibility
IsSaving (bool) — Save button loading state
IsSaveSuccess (bool) — success message visibility
SaveError (string?) — error message in Save bar

// Commands
LoadSettingsCommand — triggered from code-behind Loaded event
SaveCommand — bound to Save button
FactoryResetCommand — bound to Factory Reset button
WipeSdCardCommand — bound to SD Card Wipe button
```

Existing XAML patterns from src/BlackBoxBuddy/Views/Shell/AppShellView.axaml:
```xml
<!-- Uses FluentTheme Dark, Avalonia 12 RC1 controls -->
<!-- x:DataType for compiled bindings -->
<!-- ContentPage as tab container -->
```

From src/BlackBoxBuddy/Views/Shell/ManualConnectionDialog.axaml:
```xml
<!-- Reference for spacing: Padding="24", Spacing="16", Spacing="8" for button rows -->
<!-- Reference for styling: CornerRadius="8", FontSize="18" FontWeight="SemiBold" for titles -->
```

From src/BlackBoxBuddy/App.axaml (updated in Plan 02):
```xml
<Application.Styles>
    <FluentTheme />
    <Style Selector="Button.destructive">
      <Setter Property="Background" Value="#FFF44336"/>
      <Setter Property="Foreground" Value="White"/>
    </Style>
</Application.Styles>
```
NOTE: The `Button.destructive` style is already defined in App.axaml by Plan 02. Do NOT redefine it locally in SettingsPage.axaml. Just use `Classes="destructive"` on buttons.

Existing headless test infrastructure (from CLAUDE.md):
```csharp
// Use [AvaloniaFact] from Avalonia.Headless.XUnit
// Declare [assembly: AvaloniaTestApplication(typeof(TestApp))] once per test project
// TestApp is a minimal Application subclass with FluentTheme
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete SettingsPage XAML with all 8 sections, sticky Save bar, and EnumBooleanConverter</name>
  <files>
    src/BlackBoxBuddy/Views/SettingsPage.axaml
    src/BlackBoxBuddy/Views/SettingsPage.axaml.cs
    src/BlackBoxBuddy/Converters/EnumBooleanConverter.cs
  </files>
  <read_first>
    src/BlackBoxBuddy/Views/SettingsPage.axaml
    src/BlackBoxBuddy/Views/SettingsPage.axaml.cs
    src/BlackBoxBuddy/Views/Shell/AppShellView.axaml
    src/BlackBoxBuddy/Views/Shell/ManualConnectionDialog.axaml
    src/BlackBoxBuddy/Views/Shell/ConnectionIndicator.axaml
    src/BlackBoxBuddy/App.axaml
    .planning/phases/02-settings/02-UI-SPEC.md
  </read_first>
  <action>
    Replace the placeholder SettingsPage.axaml with the full settings UI. Follow UI-SPEC exactly for layout, spacing, typography, and colors.

    **Root layout** (per Research Pattern 3, UI-SPEC layout structure):
    ```xml
    <ContentPage xmlns="https://github.com/avaloniaui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:vm="using:BlackBoxBuddy.ViewModels"
                 xmlns:models="using:BlackBoxBuddy.Models.Settings"
                 xmlns:converters="using:BlackBoxBuddy.Converters"
                 x:Class="BlackBoxBuddy.Views.SettingsPage"
                 x:DataType="vm:SettingsViewModel">
      <Grid RowDefinitions="*,Auto">
        <!-- Row 0: Content area (loading, error, or settings scroll) -->
        <!-- Row 1: Sticky Save bar (outside ScrollViewer) -->
      </Grid>
    </ContentPage>
    ```

    **Row 0 — Content area with 3 states:**

    1. **Loading state** (`IsVisible="{Binding IsLoading}"`): Centered `StackPanel` with `ProgressRing` and `TextBlock Text="Loading settings..."` at 14px Regular.

    2. **Error state** (`IsVisible="{Binding LoadError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"`): Centered `StackPanel` with heading "Could not load settings" at 18px SemiBold and body "Check that your dashcam is connected and try again." at 14px Regular.

    3. **Settings scroll** (`IsVisible` when not loading and no error — bind to a computed negation or use a Panel with multiple children where mutual exclusivity is handled by IsVisible):
    ```xml
    <ScrollViewer Grid.Row="0">
      <StackPanel Margin="16" Spacing="32">
        <!-- 8 sections below -->
      </StackPanel>
    </ScrollViewer>
    ```

    **Section template** (repeated for each of 8 sections). Each section is:
    ```xml
    <StackPanel Spacing="16">
      <TextBlock Text="Section Name" FontSize="18" FontWeight="SemiBold"/>
      <Separator Height="1"/>
      <!-- Setting rows -->
    </StackPanel>
    ```

    **Setting row template** (label + control):
    ```xml
    <Grid ColumnDefinitions="*,Auto">
      <TextBlock Text="Setting Label" VerticalAlignment="Center" FontSize="14"/>
      <ControlType Grid.Column="1" ... />
    </Grid>
    ```

    **Section 1: WiFi** (per WIFI-01, WIFI-02, WIFI-03):
    - "Band" row: Two `RadioButton` in horizontal `StackPanel Spacing="8"`. Use EnumBooleanConverter:
      ```xml
      <RadioButton Content="2.4 GHz" GroupName="WifiBand"
                   IsChecked="{Binding WifiBand, Converter={x:Static converters:EnumBooleanConverter.Instance}, ConverterParameter={x:Static models:WifiBand.TwoPointFourGHz}}"/>
      <RadioButton Content="5 GHz" GroupName="WifiBand"
                   IsChecked="{Binding WifiBand, Converter={x:Static converters:EnumBooleanConverter.Instance}, ConverterParameter={x:Static models:WifiBand.FiveGHz}}"/>
      ```
    - "Mode" row: Two `RadioButton` — "Access Point" (WifiMode.AccessPoint) and "Client" (WifiMode.Client) using same EnumBooleanConverter
    - "Network Name" row: `TextBox PlaceholderText="Network name"` bound to `WifiSsid` two-way
    - "Password" row: `TextBox PasswordChar="*" PlaceholderText="Password"` bound to `WifiPassword` two-way

    **Section 2: Recording** (per RMOD-01..04):
    - "Driving Mode" row: RadioButton pair "Standard" / "Racing" bound to `DrivingMode` enum
    - "Parking Mode" row: RadioButton pair "Standard" / "Event Only" bound to `ParkingMode` enum

    **Section 3: Channels** (per CHAN-01, CHAN-02):
    - "Recording Channels" row: RadioButton pair "Front Only" / "Front + Rear" bound to `Channels` enum

    **Section 4: Camera** (per CAMR-01, CAMR-02):
    - "Rear Camera Orientation" row: RadioButton pair "Normal (0 degrees)" / "Flipped (180 degrees)" bound to `RearOrientation` enum

    **Section 5: Sensors** (per SENS-01..03):
    - "Driving Shock" row: `Slider Minimum="1" Maximum="5" IsSnapToTickEnabled="True" TickFrequency="1"` bound to `DrivingShockSensitivity` two-way. Show value label `TextBlock Text="{Binding DrivingShockSensitivity}"` next to slider.
    - "Parking Shock" row: Same pattern for `ParkingShockSensitivity`
    - "Radar" row: Same pattern for `RadarSensitivity`

    **Section 6: System** (per SYST-01..03):
    - "GPS" row: `ToggleSwitch OnContent="On" OffContent="Off"` bound to `GpsEnabled`
    - "Microphone" row: `ToggleSwitch OnContent="On" OffContent="Off"` bound to `MicrophoneEnabled`
    - "Speaker Volume" row: `Slider Minimum="0" Maximum="5" IsSnapToTickEnabled="True" TickFrequency="1"` bound to `SpeakerVolume`. Show value label: when value is 0, show "Disabled"; otherwise show the number. Use a simple converter or bind to a computed property.

    **Section 7: Overlays** (per OVRL-01..05):
    - "Date" row: `ToggleSwitch` bound to `DateOverlayEnabled`
    - "Time" row: `ToggleSwitch` bound to `TimeOverlayEnabled`
    - "GPS Position" row: `ToggleSwitch` bound to `GpsPositionOverlayEnabled`
    - "Speed" row: `ToggleSwitch` bound to `SpeedOverlayEnabled`
    - "Speed Unit" row: `ComboBox` with items "km/h" and "mph" bound to `SpeedUnit`. `IsVisible="{Binding SpeedOverlayEnabled}"` — only show when speed overlay is on (per UI-SPEC). Use `ItemsSource` with the two enum values and a display converter, or hardcode `ComboBoxItem` with `Tag` binding.

    **Section 8: Danger Zone** (per D-06, D-14, DNGR-01, DNGR-02):
    Wrapped in a `Border` with destructive styling per UI-SPEC:
    ```xml
    <Border BorderBrush="#FFF44336" BorderThickness="1"
            Background="#19F44336" CornerRadius="8" Padding="24">
      <StackPanel Spacing="16">
        <TextBlock Text="Danger Zone" FontSize="18" FontWeight="SemiBold" Foreground="#FFF44336"/>
        <TextBlock Text="These actions are permanent and cannot be undone."
                   FontSize="12" Opacity="0.7"/>
        <Button Content="Factory Reset..."
                Command="{Binding FactoryResetCommand}"
                Classes="destructive" HorizontalAlignment="Left"/>
        <Button Content="Wipe SD Card..."
                Command="{Binding WipeSdCardCommand}"
                Classes="destructive" HorizontalAlignment="Left"/>
      </StackPanel>
    </Border>
    ```
    NOTE: `Classes="destructive"` references the shared style in App.axaml (defined in Plan 02). Do NOT add a local `Button.destructive` style to SettingsPage.axaml.

    **Row 1 — Sticky Save bar** (per D-09, D-10):
    ```xml
    <Border Grid.Row="1"
            IsVisible="{Binding HasUnsavedChanges}"
            Background="#1EFFFFFF"
            Padding="16,8"
            Height="64">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" VerticalAlignment="Center">
        <!-- Save success/error feedback -->
        <TextBlock Text="Settings saved." IsVisible="{Binding IsSaveSuccess}"
                   Foreground="LightGreen" VerticalAlignment="Center" FontSize="14"/>
        <TextBlock Text="{Binding SaveError}" IsVisible="{Binding SaveError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                   Foreground="#FFF44336" VerticalAlignment="Center" FontSize="14"/>
        <Button Content="Save Settings"
                Command="{Binding SaveCommand}"
                Classes="accent"
                IsEnabled="{Binding !IsSaving}"/>
      </StackPanel>
    </Border>
    ```

    **Create `src/BlackBoxBuddy/Converters/EnumBooleanConverter.cs`**: A reusable IValueConverter for RadioButton-to-enum two-way binding:
    ```csharp
    namespace BlackBoxBuddy.Converters;

    public class EnumBooleanConverter : IValueConverter
    {
        public static readonly EnumBooleanConverter Instance = new();

        public object? Convert(object? value, Type targetType, object? parameter, CultureInfo culture)
            => value?.Equals(parameter) ?? false;

        public object? ConvertBack(object? value, Type targetType, object? parameter, CultureInfo culture)
            => value is true ? parameter : Avalonia.Data.BindingOperations.DoNothing;
    }
    ```

    **Update `SettingsPage.axaml.cs`** — trigger LoadSettings on Loaded event:
    ```csharp
    using Avalonia.Controls;
    using Avalonia.Interactivity;
    using BlackBoxBuddy.ViewModels;

    namespace BlackBoxBuddy.Views;

    public partial class SettingsPage : ContentPage
    {
        public SettingsPage()
        {
            InitializeComponent();
            Loaded += OnLoaded;
        }

        private void OnLoaded(object? sender, RoutedEventArgs e)
        {
            if (DataContext is SettingsViewModel vm && vm.LoadSettingsCommand.CanExecute(null))
            {
                vm.LoadSettingsCommand.Execute(null);
            }
        }
    }
    ```
    No `Ioc.Default` calls. DataContext comes from AppShellView.axaml per Plan 02.
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet build src/BlackBoxBuddy/BlackBoxBuddy.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `ScrollViewer`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `RowDefinitions="*,Auto"`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Text="WiFi"` or `Text="Wi-Fi"` (WiFi section header)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Text="Recording"` (Recording section header)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Text="Danger Zone"` (Danger Zone section header)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Binding WifiBand` (WiFi band binding)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Binding DrivingShockSensitivity` (sensor binding)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Binding GpsEnabled` (system toggle binding)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Binding SpeedOverlayEnabled` (overlay toggle binding)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Binding HasUnsavedChanges` (Save bar visibility)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Command="{Binding SaveCommand}"`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Command="{Binding FactoryResetCommand}"`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `Command="{Binding WipeSdCardCommand}"`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `BorderBrush="#FFF44336"` (Danger Zone red border)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml contains `IsSnapToTickEnabled="True"` (slider tick snapping)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml does NOT contain `Style Selector="Button.destructive"` (no local definition — uses shared App.axaml style)
    - src/BlackBoxBuddy/Views/SettingsPage.axaml.cs does NOT contain `Ioc.Default`
    - src/BlackBoxBuddy/Views/SettingsPage.axaml.cs contains `LoadSettingsCommand`
    - src/BlackBoxBuddy/Converters/EnumBooleanConverter.cs exists and contains `IValueConverter`
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>SettingsPage XAML has all 8 sections with correct controls, sticky Save bar outside ScrollViewer, Danger Zone with red border (using shared App.axaml destructive style), all bindings to SettingsViewModel properties. EnumBooleanConverter enables RadioButton-to-enum binding. Code-behind triggers LoadSettings on Loaded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EnumBooleanConverter unit tests and SettingsPage headless UI smoke test (per D-19)</name>
  <files>
    tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs
    tests/BlackBoxBuddy.Tests/Views/SettingsPageTests.cs
  </files>
  <read_first>
    src/BlackBoxBuddy/Converters/EnumBooleanConverter.cs
    src/BlackBoxBuddy/Views/SettingsPage.axaml
    src/BlackBoxBuddy/Views/SettingsPage.axaml.cs
    tests/BlackBoxBuddy.Tests/Views/Shell/AppShellViewTests.cs
  </read_first>
  <behavior>
    EnumBooleanConverter tests (plain [Fact]):
    - Test: Convert returns true when value equals parameter (e.g., WifiBand.FiveGHz == WifiBand.FiveGHz)
    - Test: Convert returns false when value does not equal parameter (e.g., WifiBand.FiveGHz != WifiBand.TwoPointFourGHz)
    - Test: Convert returns false when value is null
    - Test: ConvertBack returns parameter when value is true
    - Test: ConvertBack returns DoNothing when value is false
    - Test: ConvertBack returns DoNothing when value is null

    SettingsPage headless UI tests ([AvaloniaFact]):
    - Test: SettingsPage instantiates without error
    - Test: SettingsPage contains a ScrollViewer
    - Test: SettingsPage contains ConfirmButton-named controls (Danger Zone buttons exist)
  </behavior>
  <action>
    **Create `tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs`** (per D-19 — ValueConverter tests):
    These are plain `[Fact]` tests (no headless UI needed — converter is pure logic).

    ```csharp
    namespace BlackBoxBuddy.Tests.Converters;

    using BlackBoxBuddy.Converters;
    using BlackBoxBuddy.Models.Settings;
    using FluentAssertions;
    using System.Globalization;

    public class EnumBooleanConverterTests
    {
        private readonly EnumBooleanConverter _sut = EnumBooleanConverter.Instance;

        [Fact]
        public void Convert_WhenValueEqualsParameter_ReturnsTrue()
        {
            var result = _sut.Convert(WifiBand.FiveGHz, typeof(bool), WifiBand.FiveGHz, CultureInfo.InvariantCulture);
            result.Should().Be(true);
        }

        [Fact]
        public void Convert_WhenValueDoesNotEqualParameter_ReturnsFalse()
        {
            var result = _sut.Convert(WifiBand.FiveGHz, typeof(bool), WifiBand.TwoPointFourGHz, CultureInfo.InvariantCulture);
            result.Should().Be(false);
        }

        [Fact]
        public void Convert_WhenValueIsNull_ReturnsFalse()
        {
            var result = _sut.Convert(null, typeof(bool), WifiBand.FiveGHz, CultureInfo.InvariantCulture);
            result.Should().Be(false);
        }

        [Fact]
        public void ConvertBack_WhenValueIsTrue_ReturnsParameter()
        {
            var result = _sut.ConvertBack(true, typeof(WifiBand), WifiBand.TwoPointFourGHz, CultureInfo.InvariantCulture);
            result.Should().Be(WifiBand.TwoPointFourGHz);
        }

        [Fact]
        public void ConvertBack_WhenValueIsFalse_ReturnsDoNothing()
        {
            var result = _sut.ConvertBack(false, typeof(WifiBand), WifiBand.TwoPointFourGHz, CultureInfo.InvariantCulture);
            result.Should().Be(Avalonia.Data.BindingOperations.DoNothing);
        }

        [Fact]
        public void ConvertBack_WhenValueIsNull_ReturnsDoNothing()
        {
            var result = _sut.ConvertBack(null, typeof(WifiBand), WifiBand.TwoPointFourGHz, CultureInfo.InvariantCulture);
            result.Should().Be(Avalonia.Data.BindingOperations.DoNothing);
        }
    }
    ```

    Test with different enum types to confirm it works generically (WifiBand, DrivingMode, etc.) — add at least one test with a different enum type:
    ```csharp
    [Fact]
    public void Convert_WorksWithDifferentEnumTypes()
    {
        var result = _sut.Convert(DrivingMode.Racing, typeof(bool), DrivingMode.Racing, CultureInfo.InvariantCulture);
        result.Should().Be(true);
    }
    ```

    **Create `tests/BlackBoxBuddy.Tests/Views/SettingsPageTests.cs`** (per D-19 — View headless tests):
    Uses `[AvaloniaFact]` from `Avalonia.Headless.XUnit`. Check if `[assembly: AvaloniaTestApplication(typeof(TestApp))]` is already declared in the test project (it should be from Phase 1). If not, add it.

    ```csharp
    namespace BlackBoxBuddy.Tests.Views;

    using Avalonia.Controls;
    using Avalonia.Headless.XUnit;
    using BlackBoxBuddy.Views;
    using FluentAssertions;

    public class SettingsPageTests
    {
        [AvaloniaFact]
        public void SettingsPage_Instantiates_WithoutError()
        {
            var page = new SettingsPage();
            page.Should().NotBeNull();
        }

        [AvaloniaFact]
        public void SettingsPage_ContainsScrollViewer()
        {
            var page = new SettingsPage();
            var scrollViewer = page.FindControl<ScrollViewer>("SettingsScrollViewer");
            scrollViewer.Should().NotBeNull("SettingsPage should contain a ScrollViewer for settings content");
        }
    }
    ```

    NOTE: The ScrollViewer test requires the XAML in Task 1 to give the ScrollViewer a `Name="SettingsScrollViewer"` — if Task 1 did not include this, add it. Adjust the `SettingsPage.axaml` ScrollViewer to have `Name="SettingsScrollViewer"`.

    Ensure the tests reference the correct headless test infrastructure. If `TestApp` class exists in the test project, reuse it. If not, create a minimal one:
    ```csharp
    public class TestApp : Application
    {
        public override void Initialize() => AvaloniaXamlLoader.Load(this);
    }
    ```
  </action>
  <verify>
    <automated>cd /home/hemu/src/bbb-app && dotnet test tests/BlackBoxBuddy.Tests/ --filter "FullyQualifiedName~EnumBooleanConverter OR FullyQualifiedName~SettingsPageTests" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs exists and contains at least 6 [Fact] methods
    - tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs contains `Convert_WhenValueEqualsParameter_ReturnsTrue`
    - tests/BlackBoxBuddy.Tests/Converters/EnumBooleanConverterTests.cs contains `ConvertBack_WhenValueIsTrue_ReturnsParameter`
    - tests/BlackBoxBuddy.Tests/Views/SettingsPageTests.cs exists and contains at least 1 [AvaloniaFact] method
    - tests/BlackBoxBuddy.Tests/Views/SettingsPageTests.cs contains `SettingsPage_Instantiates_WithoutError`
    - All EnumBooleanConverter and SettingsPageTests pass (exit code 0)
  </acceptance_criteria>
  <done>EnumBooleanConverter has 7+ unit tests covering Convert/ConvertBack with matching, non-matching, and null values. SettingsPage has headless UI smoke tests confirming instantiation and ScrollViewer presence. Per D-19, ValueConverters and Views now have automated test coverage.</done>
</task>

</tasks>

<verification>
- `dotnet build src/BlackBoxBuddy/BlackBoxBuddy.csproj` succeeds
- `dotnet test tests/BlackBoxBuddy.Tests/ --filter "FullyQualifiedName~EnumBooleanConverter"` all pass
- `dotnet test tests/BlackBoxBuddy.Tests/ --filter "FullyQualifiedName~SettingsPageTests"` all pass
- SettingsPage.axaml has 8 sections with appropriate controls
- All RadioButton groups use EnumBooleanConverter for enum binding
- Save bar uses `IsVisible="{Binding HasUnsavedChanges}"`
- Danger Zone has `BorderBrush="#FFF44336"` and `Background="#19F44336"`
- Speed Unit row has `IsVisible="{Binding SpeedOverlayEnabled}"`
- SettingsPage.axaml does NOT contain a local `Button.destructive` style (uses shared App.axaml)
</verification>

<success_criteria>
- All 8 settings sections visible by scrolling (D-04, D-05)
- Every setting requirement has a corresponding control with two-way binding
- Save bar is sticky at bottom, outside ScrollViewer (D-09)
- Save bar visible only when dirty (D-10)
- Danger Zone visually separated with red styling (D-06), using shared App.axaml destructive style
- Speed Unit only visible when Speed Overlay enabled
- All sliders snap to integer ticks
- EnumBooleanConverter tested with 7+ unit tests (per D-19)
- SettingsPage smoke-tested via headless UI (per D-19)
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings/02-04-SUMMARY.md`
</output>
